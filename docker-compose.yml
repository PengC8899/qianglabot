services:
  backend:
    build: ./backend
    ports:
      - "8005:8000"
    volumes:
      - ./backend:/app
      - /app/__pycache__
    restart: always

  frontend:
    build: ./frontend
    ports:
      - "3008:80"
    depends_on:
      - backend
    restart: always
